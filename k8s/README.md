# Kubernetes Manifests

This directory contains template Kubernetes manifests (`deployment.yaml`, `service.yaml`) for deploying the Duolingo Clone frontend and backend services to Kubernetes clusters (e.g. EKS, GKE, minikube).
