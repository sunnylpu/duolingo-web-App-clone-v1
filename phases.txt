# Phase 30 — Daily Quests + Missions + Challenge System

**Hour 12 of the remaining roadmap**

We now have a substantial platform:

```text
🇬🇧 English → 8 Units → 32 Skills → 96 Lessons → 576 Exercises
🇪🇸 Spanish → 5 Units → 20 Skills → 60 Lessons → ~360 Exercises
🇫🇷 French  → 3 Units → 12 Skills → 36 Lessons → ~216 Exercises
```

And the learner now has:

```text
Learning Path
Course Switching
Lesson Engine
Hearts
XP
Streak
Daily Goal
Achievements
Leaderboard
Friends
Smart Review
Search
Vocabulary Explorer
Course/Unit Mastery
```

At this point, the next high-value feature should be a **daily mission layer**.

Why?

Because the learner currently has to decide:

> "What should I do today?"

A mature gamified learning product answers:

> "Here are today's missions."

---

# Phase 30 objective

Build:

```text
🎯 Daily Quests
🏆 Weekly Challenge
🔥 Streak Challenge
⭐ XP Challenge
📚 Learning Challenge
```

The learner should see:

```text id="8z1f2k"
Today's Quests

○ Complete 2 lessons        1 / 2
○ Earn 30 XP               20 / 30
○ Get 10 correct answers    7 / 10

[ START ]
```

And optionally:

```text
🏆 Weekly Challenge

Complete 10 lessons

7 / 10
```

---

# 1. Quest architecture

Create a new domain:

```text id="d8b8t5"
backend/app/modules/quests/
├── router.py
├── service.py
├── repository.py
├── models.py
├── schemas.py
└── tests/
```

Keep it separate from achievements.

Why?

```text
Achievement
→ permanent milestone

Quest
→ temporary objective
```

---

# 2. Quest model

Create something like:

```text id="j8j3yu"
Quest
----------------
id
code
title
description
quest_type
target_value
reward_xp
course_id nullable
active_from
active_until
```

And a learner state:

```text id="hsriqg"
UserQuest
----------------
id
user_id
quest_id
current_value
completed
completed_at
```

Unique:

```text id="t5j0c9"
(user_id, quest_id)
```

---

# 3. Quest types

Start with a small but useful set:

```text id="m3p1z0"
LESSONS_COMPLETED
XP_EARNED
CORRECT_ANSWERS
SKILLS_COMPLETED
REVIEWS_COMPLETED
```

Examples:

```text id="1o9s2w"
Complete 2 lessons

Earn 30 XP

Answer 10 questions correctly

Complete 1 skill

Complete 3 review exercises
```

---

# 4. Daily quest generation

Each day generate **3 quests** per learner.

Example:

```text id="b77q9b"
Quest 1:
Complete 2 lessons
Reward: +20 XP

Quest 2:
Earn 30 XP
Reward: +15 XP

Quest 3:
Answer 10 correctly
Reward: +20 XP
```

Do not generate them randomly every API request.

They must be stable for the day.

---

# 5. Deterministic daily quests

Use the user's:

```text id="9x6w3e"
user_id
+
date
```

to deterministically select/generate that day's quests.

This prevents:

```text id="n8g8fk"
refresh page
→ quests change
```

The same day should show the same missions.

---

# 6. Daily reset

Don't use a frontend midnight timer.

Use the backend's existing application timezone:

```text id="4wz1oa"
APP_TIMEZONE
```

When the date changes:

```text id="4g9o4s"
new DailyActivity
→ new daily quests
```

No cron job is required for the MVP.

---

# 7. Quest progress

Quest progress should update automatically based on existing events.

For example:

```text id="nyk1zj"
Lesson completed
      ↓
LESSONS_COMPLETED += 1
XP_EARNED += lesson XP
```

And:

```text id="g0zgpf"
Correct exercise
      ↓
CORRECT_ANSWERS += 1
```

Don't ask the frontend to call:

```text id="9y0g1c"
POST /quests/progress
```

after every action.

The backend should derive quest progress from the actual domain event.

---

# 8. Quest completion

When:

```text id="jsc8ku"
current_value >= target_value
```

set:

```text id="c0yuo8"
completed = true
```

and award:

```text id="i0n5hr"
reward_xp
```

exactly once.

---

# 9. Prevent quest reward duplication

Critical.

Example:

```text id="8c9x5u"
Quest target = 2 lessons

Lesson 1
→ 1 / 2

Lesson 2
→ 2 / 2
→ +20 XP

retry same completion
→ +0
```

Use:

```text id="gv2f9z"
UserQuest.completed
+
database constraints
```

---

# 10. Quest reward transaction

When an action completes a quest:

```text id="uq2r27"
BEGIN

complete lesson
update XP
update daily activity
update quests
award quest XP

COMMIT
```

This keeps quest progress consistent with the event that caused it.

---

# 11. Weekly challenge

Create a second layer:

```text id="8h1k4g"
Weekly Challenge
```

Example:

```text id="7twcya"
🏆 Weekly Challenge

Complete 10 lessons

7 / 10

Reward:
+100 XP
```

One active weekly challenge is enough for this phase.

---

# 12. Weekly challenge model

You can reuse:

```text id="qu6yq6"
Quest
```

with:

```text id="quest_scope"
daily
weekly
```

rather than creating a completely separate table.

This keeps the model simple.

---

# 13. Course-aware quests

Daily quests should optionally respect the selected course.

Example:

```text id="i4x7ry"
Selected course: Spanish

Complete 2 Spanish lessons
```

But don't force every quest to be course-specific.

Use:

```text id="2y22i1"
global quests
```

and:

```text id="e8yn3g"
course-specific quests
```

mixed carefully.

---

# 14. Quest API

Create:

```text id="v4z6m8"
GET /api/v1/quests/today
```

Response:

```json id="77f2aj"
{
  "date": "2026-08-14",
  "quests": [
    {
      "id": 1,
      "title": "Complete 2 lessons",
      "current": 1,
      "target": 2,
      "reward_xp": 20,
      "completed": false
    }
  ]
}
```

And:

```text id="j3u5hb"
GET /api/v1/quests/weekly
```

---

# 15. No manual quest-progress endpoint

Avoid:

```text id="0z3s7l"
POST /quests/update
```

The frontend should only read quest state.

Domain actions update it automatically.

---

# 16. Quest UI

Create:

```text id="6a6wep"
frontend/features/quests/
├── components/
│   ├── DailyQuestCard.tsx
│   ├── DailyQuestList.tsx
│   ├── QuestProgress.tsx
│   ├── WeeklyChallenge.tsx
│   └── QuestCompleteModal.tsx
├── hooks/
│   └── useQuests.ts
└── services/
    └── quest-service.ts
```

---

# 17. Daily quest card

Example:

```text id="h2h2p9"
Today's Quests

┌────────────────────────────────┐
│ 📚 Complete 2 lessons          │
│ ██████████░░░░ 1 / 2          │
│ +20 XP                         │
└────────────────────────────────┘

┌────────────────────────────────┐
│ ⭐ Earn 30 XP                  │
│ █████████████░ 20 / 30        │
│ +15 XP                         │
└────────────────────────────────┘
```

---

# 18. Quest completion animation

When completed:

```text id="p6p4j4"
✓ Quest Complete!

+20 XP
```

Use the existing reward queue rather than inventing another modal framework.

---

# 19. Home dashboard integration

The Home page should now have:

```text id="c4mtjv"
Continue Learning

Daily Goal

Today's Quests

Smart Review

Your Courses
```

Quest cards should be visible but not dominate the screen.

---

# 20. Profile integration

Add:

```text id="urpyx6"
Quest Statistics

42 quests completed
8 weekly challenges completed
```

Only if the data is easy to calculate.

Don't introduce unnecessary analytics tables.

---

# 21. Quest + streak interaction

Completing quests does not directly increase streak.

The rule remains:

```text id="l54w0q"
Learning activity
→ streak

Quest completion
→ reward XP
```

The quest is observing learning behavior.

---

# 22. Quest + achievements

This is where the systems become interesting.

You can later have achievements like:

```text id="6z3m20"
QUEST_STARTER
Complete 10 daily quests

QUEST_MASTER
Complete 50 daily quests
```

For this phase, the data model should make this possible, but don't needlessly expand the achievement catalog again.

---

# 23. Quest + leaderboard

Quest rewards become XP.

Therefore:

```text id="b8jjsn"
Quest completed
→ +XP
→ DailyActivity
→ leaderboard XP
```

This happens automatically through the existing XP/gamification flow.

Do not create:

```text id="0t4j4f"
POST leaderboard/update
```

---

# 24. Quest difficulty

Daily quest targets should stay achievable.

Examples:

```text id="u7y6zq"
Beginner learner:
2 lessons
20 XP

Advanced learner:
3 lessons
40 XP
```

You may choose one deterministic difficulty based on learner level.

Don't make quests impossible.

---

# 25. Quest personalization

Use the learner's current activity where practical.

For example, if the learner is actively learning Spanish:

```text id="w7rv4j"
Complete 2 Spanish lessons
```

But avoid highly complex personalization.

A simple rule:

```text id="fqz2i0"
selected course → course-specific quest
```

is sufficient.

---

# 26. Quest expiration

Daily:

```text id="7zwtaj"
expires at next application day
```

Weekly:

```text id="b8bq2f"
expires at end of week
```

Completed quests remain visible as historical completion for the period.

---

# 27. Quest history

Optional but useful:

```text id="mbouu0"
GET /api/v1/quests/history
```

Return recent completed quests.

Example:

```text id="k6jw39"
Yesterday:
✓ Complete 2 lessons
✓ Earn 30 XP

Monday:
✓ Complete 3 reviews
```

Keep it limited to recent history.

---

# 28. Backend tests

Test:

```text id="xql1mu"
daily quests stable for same date
new day creates new quests
quest progress updates
quest completion
quest XP
duplicate completion
weekly challenge
course-specific quest
global quest
timezone behavior
```

---

# 29. Integration tests

Test:

```text id="o6t9n6"
Complete lesson
→ quest progress changes

Answer correctly
→ correct-answer quest changes

Earn XP
→ XP quest changes

Complete quest
→ XP increases

Leaderboard reflects earned XP
```

This demonstrates the event flow.

---

# 30. Frontend tests

Test:

```text id="crj20v"
quest cards
progress
completion state
weekly challenge
loading
error
home integration
```

---

# 31. E2E journey

Create one impressive E2E:

```text id="k8aeyp"
Open Home
↓
See 3 Daily Quests
↓
Start lesson
↓
Complete lesson
↓
Quest progress increases
↓
Earn enough XP
↓
Quest completes
↓
+Quest XP
↓
Quest becomes ✓ Complete
```

This is an excellent demo scenario.

---

# 32. Phase 30 acceptance criteria

```text id="e9x8xs"
[ ] Quest module exists
[ ] Daily quest model exists
[ ] UserQuest progress exists
[ ] Daily quest generation is deterministic
[ ] Daily quests remain stable throughout the day
[ ] Daily quests reset on new application date
[ ] Weekly challenge exists
[ ] Quest progress updates from real domain events
[ ] Frontend cannot manually manipulate quest progress
[ ] Quest completion awards XP exactly once
[ ] Quest XP flows into existing gamification
[ ] Quest XP flows into leaderboard activity
[ ] Course-specific quests are supported
[ ] Daily quest API works
[ ] Weekly challenge API works
[ ] Quest UI works
[ ] Quest completion feedback works
[ ] Home dashboard integrates quests
[ ] Backend tests pass
[ ] Frontend tests/build pass
[ ] Integration tests pass
[ ] E2E quest journey passes
```

---

# What Phase 30 does NOT implement

```text id="gzgrb2"
❌ Real notifications
❌ Push notifications
❌ Chat
❌ ML personalization
❌ AI-generated quests
❌ Payments
❌ Authentication
❌ Docker
❌ Jenkins
❌ Kubernetes
❌ AWS
```

---

## Product result after Hour 12

The daily user loop now becomes:

```text id="4p4h2m"
              OPEN APP
                  │
                  ▼
             DAILY QUESTS
                  │
        ┌─────────┼─────────┐
        ▼         ▼         ▼
      LEARN     REVIEW   PRACTICE
        │         │         │
        └─────────┼─────────┘
                  ▼
                 XP
                  │
        ┌─────────┼──────────┐
        ▼         ▼          ▼
      STREAK   ACHIEVEMENT  LEADERBOARD
                  │
                  ▼
              MORE GOALS
```

This is a particularly valuable addition because it creates a **daily gameplay loop** without introducing an unnecessary new technical platform.
# Phase 31 — Notifications + Reminder System + Daily Engagement

**Hour 13 of the remaining roadmap**

We now have a strong daily loop:

```text
Home
 ↓
Daily Quests
 ↓
Learn / Review
 ↓
XP
 ↓
Streak
 ↓
Achievements
 ↓
Leaderboard
```

But the learner still has to **remember to come back**.

Phase 31 adds a lightweight notification/reminder system without introducing push infrastructure yet.

The goal is:

```text
🔔 Daily reminder
🔥 Streak reminder
🎯 Quest reminder
🏆 Achievement notification
📚 Course progress reminder
```

The system should be designed so that later, when we deploy on AWS, it can evolve into email/push notifications without rewriting the core domain.

---

# 1. Notification architecture

Create:

```text id="f65x9j"
backend/app/modules/notifications/
├── router.py
├── service.py
├── repository.py
├── models.py
├── schemas.py
└── tests/
```

Responsibilities:

```text id="q1z3ue"
notification creation
notification retrieval
read/unread state
notification preferences
reminder scheduling metadata
```

Do not put streak or XP logic here.

Notifications observe events generated by other domains.

---

# 2. Notification model

Create:

```text id="8rxr0m"
Notification
----------------
id
user_id
type
title
message
metadata
is_read
created_at
```

Potential notification types:

```text id="7h1pn9"
DAILY_REMINDER
STREAK_REMINDER
QUEST_REMINDER
ACHIEVEMENT_UNLOCKED
UNIT_COMPLETED
COURSE_COMPLETED
SOCIAL_ACTIVITY
```

Use JSON metadata for contextual data.

Example:

```json id="x8g0tq"
{
  "course_id": "crs_english",
  "skill_id": "skill_questions"
}
```

---

# 3. Notification API

Create:

```text id="f0h3x8"
GET /api/v1/notifications
```

Optional:

```text id="95y1f4"
?unread_only=true
?limit=20
```

Response:

```json id="xj0j8g"
{
  "items": [
    {
      "id": 101,
      "type": "ACHIEVEMENT_UNLOCKED",
      "title": "Achievement Unlocked!",
      "message": "You earned 7 Day Streak",
      "is_read": false,
      "created_at": "..."
    }
  ],
  "unread_count": 1
}
```

---

# 4. Mark notification read

Implement:

```text id="q2o7g7"
POST /api/v1/notifications/{id}/read
```

The current user must own the notification.

Do not accept arbitrary:

```text id="9u9r2v"
user_id
```

from the frontend.

---

# 5. Mark all read

Useful:

```text id="bm6y6e"
POST /api/v1/notifications/read-all
```

This should only affect the current user's notifications.

---

# 6. Notification preferences

Create:

```text id="ij3v7d"
NotificationPreference
----------------
user_id
daily_reminders
streak_reminders
quest_reminders
social_notifications
achievement_notifications
```

One row per user.

Default:

```text id="wr8mzw"
all enabled
```

---

# 7. Preferences API

Implement:

```text id="6x9l1t"
GET /api/v1/notifications/preferences
PATCH /api/v1/notifications/preferences
```

Example:

```json id="2n5n0m"
{
  "daily_reminders": true,
  "streak_reminders": true,
  "quest_reminders": true,
  "social_notifications": false,
  "achievement_notifications": true
}
```

This makes your future notification system configurable.

---

# 8. Important: no fake background scheduler

Do **not** build:

```text id="8grx94"
Celery
Redis
cron
Kafka
```

yet.

We're still preparing the product.

Instead create:

```text id="3o2dqr"
ReminderCandidate
```

or a service that can determine:

```text id="3x4mqp"
who should be reminded
```

Later Jenkins/AWS can invoke it.

---

# 9. Reminder eligibility service

Create:

```text id="l0eqxg"
notifications/reminder_service.py
```

Functions:

```text id="0d5jws"
get_daily_reminder_candidates()
get_streak_reminder_candidates()
get_quest_reminder_candidates()
```

These return users who are eligible.

---

# 10. Daily reminder rule

Simple rule:

```text id="j7xk3c"
user has no learning activity today
+
daily_reminders enabled
→ candidate
```

Do not send multiple reminders every time the API is called.

Use a reminder record/idempotency mechanism.

---

# 11. Streak reminder rule

If:

```text id="4g4s4v"
current streak > 0
+
no activity today
```

then:

```text id="5c8o0v"
streak reminder candidate
```

Example message:

```text id="v1ae7f"
🔥 Your 12-day streak is waiting!

Complete a lesson today to keep it alive.
```

---

# 12. Quest reminder rule

If:

```text id="q6h3xg"
daily quests incomplete
+
quest_reminders enabled
```

then:

```text id="87ckk3"
quest reminder candidate
```

Example:

```text id="p4sv5k"
🎯 You still have 2 quests left today.
```

---

# 13. Achievement notification

When an achievement is earned:

```text id="t3h1ry"
AchievementEngine
   ↓
NotificationService
```

Create:

```text id="x9jpsu"
ACHIEVEMENT_UNLOCKED
```

This should happen as part of the existing lesson/reward transaction where appropriate.

---

# 14. Unit/course completion notifications

When a unit completes:

```text id="1e9jzo"
🎉 Unit Complete!

You completed Foundations.
```

When a course completes:

```text id="2y8drq"
🎓 English Mastery Complete!

You finished the English course.
```

Again, notification creation should be tied to real backend events.

---

# 15. Social notifications

When someone you follow achieves something significant:

```text id="z0aplc"
Alex completed Unit 3.
```

Keep social notifications limited.

Do not notify on:

```text id="v6yrj4"
every lesson
every exercise
every XP change
```

That would create noise.

---

# 16. Notification frontend

Create:

```text id="j2a0q5"
frontend/features/notifications/
├── components/
│   ├── NotificationBell.tsx
│   ├── NotificationPanel.tsx
│   ├── NotificationItem.tsx
│   └── NotificationPreferences.tsx
├── hooks/
│   └── useNotifications.ts
└── services/
    └── notification-service.ts
```

---

# 17. Notification bell

Add to header:

```text id="7q5hcl"
🔔
```

Unread:

```text id="0f6n4c"
🔔 3
```

or a small badge:

```text id="b0g7tj"
🔴 3
```

Don't rely solely on red color.

Use accessible text:

```text id="3n81h7"
3 unread notifications
```

---

# 18. Notification panel

Example:

```text id="6d0zva"
┌─────────────────────────────────────┐
│ Notifications                  ✓    │
├─────────────────────────────────────┤
│ 🏆 Achievement Unlocked             │
│ 7 Day Streak                        │
│ 2h ago                              │
│                                     │
│ 🎯 Daily Quest                      │
│ Complete 1 more lesson              │
│ 5h ago                              │
│                                     │
│ 🔥 Streak Reminder                  │
│ Keep your 12 day streak             │
└─────────────────────────────────────┘
```

---

# 19. Read state

Unread:

```text id="f6vlwq"
bold
badge
```

Read:

```text id="a7m4nt"
normal
```

Clicking a notification:

```text id="0a1u0k"
marks it read
```

and optionally navigates to the relevant object.

---

# 20. Deep-link metadata

Notifications should carry enough metadata to navigate.

Examples:

```text id="xj0ypo"
achievement:
achievement_code

unit:
course_id
unit_id

lesson:
course_id
lesson_id

social:
user_id
```

The frontend maps these to known application routes.

---

# 21. No notification duplication

Important.

If:

```text id="nmtf44"
Achievement unlocked
```

already produced a notification, don't produce another one because of a retry.

Use an idempotency key such as:

```text id="7zj2ka"
user_id
event_type
entity_type
entity_id
```

where appropriate.

---

# 22. Notification preference UX

Add under Settings:

```text id="b1ko2j"
Notifications

☑ Daily reminders
☑ Streak reminders
☑ Quest reminders
☑ Achievement notifications
☐ Social notifications
```

Use real backend preferences.

---

# 23. Settings page

The previous Settings placeholder can now become useful.

Keep it lightweight:

```text id="9xg8ey"
Profile
Notifications
Appearance
Language
```

Only Notifications is functional now.

Everything else can remain placeholder.

---

# 24. Reminder scheduling API

You don't need a public endpoint that lets the browser say:

```text id="zq4r0x"
send me a reminder now
```

Instead the backend should expose an internal/service-level method.

Conceptually:

```text id="3mk6s9"
NotificationService.generate_daily_reminders()
```

Later:

```text id="m2gcd0"
AWS EventBridge / CloudWatch
→ invoke reminder job
```

This keeps today's code cloud-ready.

---

# 25. Reminder deduplication

Create a simple record:

```text id="k7lh7m"
NotificationDelivery
--------------------
id
user_id
notification_type
reference_date
created_at
```

Unique:

```text id="4m3t1y"
(user_id, notification_type, reference_date)
```

For example:

```text id="v1erj0"
Sunny
STREAK_REMINDER
2026-08-14
```

can only be generated once that day.

---

# 26. Local testing endpoint

For development only, you can add:

```text id="2sqc4k"
POST /api/v1/dev/notifications/generate
```

but **do not expose this in production**.

Better: use a Python CLI:

```bash id="7b9xwi"
python -m notifications.generate_reminders
```

This is safer and will later map nicely into a deployment job.

---

# 27. Notifications + quest system

Daily quests can produce:

```text id="4vxy3a"
QUEST_REMINDER
```

and:

```text id="5g7r7g"
QUEST_COMPLETED
```

Don't generate a notification for every quest progress increment.

Only:

```text id="5k5a2j"
important milestone
```

---

# 28. Notifications + streak

When a streak is about to be lost:

```text id="mkvbux"
Streak reminder
```

When a milestone is achieved:

```text id="8t3g2l"
Streak milestone
```

These are separate events.

---

# 29. Notification history

The user should be able to scroll older notifications:

```text id="0zqjca"
Today
Yesterday
Earlier
```

Use pagination.

No need to load thousands of rows at once.

---

# 30. Testing — notification engine

Backend:

```text id="2rdj2u"
create notification
read notification
read all
preferences
daily reminder eligibility
streak reminder eligibility
quest reminder eligibility
achievement notification
unit notification
course notification
deduplication
ownership
```

---

# 31. Testing — reminder rules

Test:

```text id="jq5k9u"
no activity today
→ daily reminder candidate

activity today
→ no daily reminder

streak > 0 + no activity
→ streak candidate

quest incomplete
→ quest candidate

all quests completed
→ no quest reminder
```

---

# 32. Frontend tests

Test:

```text id="t4k9a6"
bell
unread badge
notification panel
read notification
read all
preferences
navigation
empty state
```

---

# 33. E2E notification journey

Test:

```text id="1qj529"
create seeded unread notification
↓
open app
↓
bell shows badge
↓
open panel
↓
click notification
↓
notification becomes read
```

This proves the system is functional without needing real push infrastructure.

---

# 34. Phase 31 acceptance criteria

```text id="d8d3l4"
[ ] Notification module exists
[ ] Notification model exists
[ ] User preferences exist
[ ] Notification API works
[ ] Mark-read works
[ ] Mark-all-read works
[ ] Unread count works
[ ] Notification bell works
[ ] Notification panel works
[ ] Daily reminder eligibility works
[ ] Streak reminder eligibility works
[ ] Quest reminder eligibility works
[ ] Achievement notification works
[ ] Unit notification works
[ ] Course notification works
[ ] Notification deduplication works
[ ] Notification ownership is enforced
[ ] Settings notifications are functional
[ ] Reminder logic uses application timezone
[ ] No background scheduler is required for current MVP
[ ] Future scheduler integration point exists
[ ] Backend tests pass
[ ] Frontend tests/build pass
[ ] E2E notification flow passes
```

---

# What this phase does NOT implement

```text id="hmf1oj"
❌ Firebase
❌ Web Push
❌ Email provider
❌ SMS
❌ AWS EventBridge implementation
❌ Celery
❌ Redis
❌ Kafka
❌ Real-time WebSockets
❌ Authentication
❌ Docker
❌ Jenkins
❌ Kubernetes
❌ AWS
```

## Result after Hour 13

The learner lifecycle becomes:

```text id="uyn3v6"
                    LEARNER
                       │
                       ▼
                    ACTIVITY
                       │
       ┌───────────────┼────────────────┐
       ▼               ▼                ▼
     Quests         Streak          Achievements
       │               │                │
       └───────────────┼────────────────┘
                       ▼
                 Notifications
                       │
             ┌─────────┴─────────┐
             ▼                   ▼
         Come back            Continue
```

This is the last major **engagement** layer I'd add before we shift toward technical production work. The next hour should start tightening the architecture around **observability, audit trails, API health, and production-grade operational tooling** rather than adding endless UI features.

# Phase 32 — Production Observability + Audit Trail + Operational Dashboard

**Hour 14 of the remaining roadmap**

We have now built almost the entire product ecosystem:

```text
Courses
Units
Skills
Lessons
Exercises
Progression
Mastery
XP
Hearts
Streaks
Quests
Achievements
Leaderboard
Friends
Notifications
Search
Vocabulary
Smart Review
```

At this point, **stop adding user-facing features for a while**.

The next requirement is to make the system look and behave like something that could actually be operated in production.

This phase creates the foundation for:

```text
Application
    ↓
Logs
    ↓
Metrics
    ↓
Audit Events
    ↓
Health
    ↓
Operational Dashboard
```

This is especially important because your next deployment stages are:

```text
Docker
→ Jenkins
→ Kubernetes
→ AWS
```

Kubernetes without health/metrics/logging is just containers running blindly.

---

# Phase 32 objectives

Implement:

```text
1. Structured logging
2. Request correlation
3. Audit/event trail
4. Application metrics
5. Health/readiness checks
6. Operational metrics endpoint
7. Admin/ops dashboard foundation
```

Do **not** introduce Prometheus/Grafana infrastructure yet.

We will prepare the application so Phase 36+ deployment can plug those systems in cleanly.

---

# 1. Structured logging

Review:

```text
backend/app/shared/logging.py
```

Make logs structured and consistent.

Every request should be able to produce information such as:

```json
{
  "timestamp": "...",
  "level": "INFO",
  "service": "api",
  "request_id": "req_abc123",
  "method": "POST",
  "path": "/api/v1/lessons/12/complete",
  "status_code": 200,
  "duration_ms": 124
}
```

Do not log giant request/response bodies.

---

# 2. Never log sensitive data

Explicitly prevent logging:

```text
passwords
tokens
authorization headers
full exercise answers where inappropriate
private user information
database credentials
API keys
```

Especially avoid:

```python
print(request.json())
```

in production code.

---

# 3. Request ID middleware

Your project already has middleware infrastructure.

Ensure:

```text
X-Request-ID
```

exists for every request.

If the client provides a request ID, validate/use it appropriately.

Otherwise generate one.

Example:

```text
Request:
POST /answer

Request-ID:
req_82f71c
```

Every relevant log entry generated during that request should contain:

```text
req_82f71c
```

---

# 4. Response header

Return:

```text
X-Request-ID: req_82f71c
```

This gives you:

```text
browser
→ API
→ logs
```

traceability.

This becomes extremely useful once Kubernetes introduces multiple replicas.

---

# 5. Request timing middleware

Measure:

```text
request start
request end
duration_ms
```

Log slow requests.

For example:

```text
> 500ms
```

could generate:

```text
WARN slow_request
```

Make the threshold configurable:

```text
SLOW_REQUEST_THRESHOLD_MS=500
```

---

# 6. Application metrics

Create an in-process metrics abstraction.

At minimum track:

```text id="z02d4t"
requests_total
request_errors_total
request_duration
lesson_starts_total
lesson_completions_total
exercise_answers_total
exercise_correct_total
exercise_incorrect_total
heart_losses_total
xp_awarded_total
quests_completed_total
achievements_unlocked_total
```

Don't create dozens of metrics.

Focus on business-critical events.

---

# 7. Metrics architecture

Create:

```text
backend/app/shared/metrics.py
```

with a small interface such as:

```text
increment()
observe()
count()
```

The application shouldn't directly know whether metrics eventually go to:

```text
Prometheus
CloudWatch
Datadog
```

This abstraction will make deployment easier.

---

# 8. Metrics endpoint

Create:

```text
GET /metrics
```

For now, return a simple internal representation if Prometheus isn't installed yet.

However, preferably make the output **Prometheus-compatible** if practical.

Example:

```text
duolingo_requests_total 1520
duolingo_lesson_completions_total 83
duolingo_exercise_answers_total 620
```

Then later Prometheus can scrape it directly.

---

# 9. Health endpoints

Ensure these exist:

```text
GET /health/live
GET /health/ready
GET /health
```

### Live

Answers:

> Is the process running?

```json
{
  "status": "ok"
}
```

No database query needed.

---

# 10. Readiness

`/health/ready` should check required dependencies.

For now:

```text
database accessible
```

Example:

```json
{
  "status": "ready",
  "checks": {
    "database": "ok"
  }
}
```

If the database fails:

```json
{
  "status": "not_ready",
  "checks": {
    "database": "error"
  }
}
```

Use an appropriate non-success HTTP status.

This endpoint will later become the Kubernetes readiness probe.

---

# 11. Health response metadata

It is useful to expose:

```text
application version
environment
timestamp
```

Example:

```json
{
  "status": "ready",
  "version": "1.0.0",
  "environment": "production",
  "checks": {
    "database": "ok"
  }
}
```

Do not expose secrets or internal infrastructure information.

---

# 12. Application version

Introduce:

```text
APP_VERSION=1.0.0
```

or derive it from a build/version file.

The same value can later appear in:

```text
logs
health
Docker image
deployment
```

---

# 13. Audit trail

Now create a generic audit/event model.

Something like:

```text
AuditEvent
----------------
id
user_id nullable
event_type
entity_type
entity_id
metadata
created_at
request_id
```

Examples:

```text
LESSON_COMPLETED
SKILL_COMPLETED
UNIT_COMPLETED
COURSE_COMPLETED
ACHIEVEMENT_EARNED
QUEST_COMPLETED
FOLLOW_CREATED
PROFILE_UPDATED
```

---

# 14. Audit vs activity feed

Important distinction.

### ActivityEvent

```text
user-facing social history
```

### AuditEvent

```text
internal operational history
```

Do not use the same table for both.

The audit trail can answer:

> What happened in the system and when?

The activity feed answers:

> What should the learner/social graph see?

---

# 15. Audit service

Create:

```text
backend/app/shared/audit.py
```

or equivalent.

Provide:

```text
record_event()
```

with:

```text
user
event type
entity
metadata
request_id
```

The application should call this from domain workflows.

---

# 16. High-value events

Do not audit every exercise click.

Record high-value transitions:

```text
lesson_started
lesson_completed
skill_completed
unit_completed
course_completed
achievement_earned
quest_completed
heart_refilled
practice_recovery
follow_created
follow_removed
```

This provides meaningful operational history.

---

# 17. Audit consistency

When possible, create the audit event in the **same transaction** as the domain action.

Example:

```text
Lesson completion
 ├── XP
 ├── progress
 ├── achievement
 └── audit event

COMMIT
```

If the transaction rolls back:

```text
no audit event
```

This prevents audit history from claiming something happened when the underlying transaction failed.

---

# 18. Admin/operations statistics

Create a read-only endpoint:

```text
GET /api/v1/admin/overview
```

Since real admin authentication isn't implemented yet, keep it strictly development/internal for now and clearly document that it must be protected before production.

Return aggregate information such as:

```json
{
  "users": {
    "total": 24,
    "active_today": 9
  },
  "courses": {
    "total": 3
  },
  "learning": {
    "lessons_completed_today": 47,
    "exercises_answered_today": 381
  },
  "gamification": {
    "xp_awarded_today": 920,
    "achievements_unlocked_today": 12
  },
  "system": {
    "requests_today": 18240,
    "errors_today": 34
  }
}
```

Do not expose personal data.

---

# 19. Operations dashboard

Create a lightweight internal page:

```text
/admin
```

or:

```text
/ops
```

Display:

```text
System Health
API Requests
Error Rate
Active Users
Lessons Completed
Exercise Accuracy
XP Awarded
Achievements
```

Example:

```text
┌────────────────────────────────────────────┐
│ Operations Dashboard                       │
├────────────────────────────────────────────┤
│ API Health          ● Healthy              │
│ Database            ● Healthy              │
│                                            │
│ Requests Today        18,240              │
│ Errors                    34              │
│ Lesson Completions        47              │
│ XP Awarded               920              │
│                                            │
│ Exercise Accuracy       84%               │
└────────────────────────────────────────────┘
```

This is primarily a portfolio/evaluation feature.

---

# 20. Frontend ops structure

Create:

```text
frontend/features/ops/
├── components/
│   ├── SystemHealthCard.tsx
│   ├── MetricCard.tsx
│   ├── ActivityMetric.tsx
│   └── OpsDashboard.tsx
├── hooks/
│   └── useOpsOverview.ts
└── services/
    └── ops-service.ts
```

Keep it separate from learner-facing components.

---

# 21. Metrics useful for evaluation

Show:

```text
API Requests
API Errors
Average Response Time
Lessons Completed
Exercises Answered
Correct Answer %
XP Awarded
Achievements Earned
```

This gives you something concrete to discuss in an interview:

> "We don't just deploy the API; we can observe its business and infrastructure behavior."

---

# 22. Error tracking

Standardize unexpected server errors.

When an exception occurs, log:

```text
request_id
exception type
path
method
stack trace
```

but return the client only:

```json
{
  "error": {
    "code": "INTERNAL_ERROR",
    "message": "An unexpected error occurred."
  }
}
```

Never return the Python traceback to users.

---

# 23. Structured business-event logs

Important events can additionally emit structured logs:

```json
{
  "event": "lesson_completed",
  "user_id": 1,
  "lesson_id": 42,
  "course_id": "crs_english",
  "xp": 20,
  "request_id": "req_123"
}
```

Be careful with user identifiers and privacy.

Use internal IDs rather than unnecessary personal data.

---

# 24. Database audit indexes

Add indexes for:

```text
AuditEvent.user_id
AuditEvent.event_type
AuditEvent.created_at
AuditEvent.request_id
```

Similarly ensure metrics/event queries have useful indexes.

---

# 25. Operational retention

Don't keep infinite audit data in SQLite in the MVP.

Document a future retention policy, for example:

```text
activity feed:
90 days

audit:
180 days

metrics:
external time-series system
```

No need to implement automated cleanup yet.

---

# 26. Correlation across domains

A single lesson completion should be traceable:

```text
request_id
   ↓
LessonCompleted
   ↓
XP
   ↓
SkillProgress
   ↓
UnitMilestone
   ↓
Achievement
   ↓
Notification
```

Each domain can use the same request ID.

This becomes extremely valuable when you later split services.

---

# 27. Future microservice readiness

We previously discussed treating modules as future services.

Now the event flow can eventually become:

```text
Lesson Service
      │
      │ LessonCompleted
      ▼
Event Bus
 ┌────┼──────┬────────┐
 ▼    ▼      ▼        ▼
XP   Progress Achievement Notifications
```

**Do not implement the event bus now.**

Just make the event/audit boundaries explicit.

---

# 28. Test health endpoints

Test:

```text
/health/live → 200
/health/ready → 200 when DB works
/health/ready → non-200 when DB unavailable
```

---

# 29. Test metrics

Perform:

```text
GET /metrics
```

after:

```text
lesson completion
exercise answer
quest completion
achievement
```

Verify counters increase correctly.

---

# 30. Test audit

Complete a lesson:

```text
lesson_completed
```

Verify an AuditEvent exists with:

```text
user
entity
event type
request ID
timestamp
```

Repeat a failed transaction and verify:

```text
no misleading committed audit event
```

---

# 31. Test notification correlation

Trigger:

```text
achievement earned
```

Verify:

```text
achievement record
notification
audit event
```

all correlate to the same request/workflow.

---

# 32. Performance impact

Observability itself must not destroy performance.

Logging should be:

```text
structured
minimal
non-blocking where practical
```

Metrics should use cheap in-process counters.

Audit writes should be transactional but not unnecessarily duplicated.

---

# 33. Phase 32 acceptance criteria

```text
[ ] Structured logging exists
[ ] Request ID exists
[ ] X-Request-ID response header exists
[ ] Request duration is measured
[ ] Slow-request threshold is configurable
[ ] Sensitive information is excluded from logs
[ ] Metrics abstraction exists
[ ] Business metrics exist
[ ] /metrics endpoint exists
[ ] /health/live exists
[ ] /health/ready exists
[ ] readiness checks database
[ ] APP_VERSION exists
[ ] AuditEvent model exists
[ ] Audit service exists
[ ] High-value domain events are audited
[ ] Audit events use request correlation
[ ] Audit events respect transactions
[ ] Ops overview API exists
[ ] Ops dashboard exists
[ ] Error handling doesn't expose stack traces
[ ] Metrics tests pass
[ ] Health tests pass
[ ] Audit tests pass
[ ] Backend tests pass
[ ] Frontend build passes
```

---

# What Phase 32 does NOT implement

```text
❌ Prometheus server
❌ Grafana
❌ CloudWatch
❌ OpenTelemetry collector
❌ Kafka
❌ RabbitMQ
❌ Redis
❌ ELK
❌ Real admin authentication
❌ Docker
❌ Jenkins
❌ Kubernetes
❌ AWS
```

Those come later.

---

# Result after Hour 14

Your architecture now becomes much closer to a production system:

```text
                    APPLICATION
                         │
          ┌──────────────┼──────────────┐
          ▼              ▼              ▼
       Business        Audit          Metrics
        Logic          Trail          / Logs
          │              │              │
          └──────────────┼──────────────┘
                         ▼
                  Health / Readiness
                         │
                         ▼
                  Deployment Layer
```

And later:

```text
Docker
   ↓
Jenkins
   ↓
Kubernetes
   ↓
Prometheus/Grafana
   ↓
AWS
```

This is the right transition point: **we have spent 14 hours making the product deep; now the remaining hours should increasingly make it deployable, observable, secure, and production-operable.**

# Phase 33 — Production Security, Configuration Hardening & API Protection

**Hour 15 of the remaining roadmap**

Phase 32 established observability:

```text
Logs
Metrics
Audit
Health
Readiness
Request IDs
Ops dashboard
```

Now we make the application **safe to expose to the internet**.

This is the last major backend-hardening phase before we start preparing the actual deployment stack.

The focus is:

```text
Configuration
→ Secrets
→ CORS
→ Security headers
→ API validation
→ Rate limiting foundation
→ Database safety
→ Production error handling
→ Dependency/security audit
```

---

# 1. Environment configuration audit

Review:

```text id="1v6yjr"
backend/app/config.py
frontend/.env*
```

Move every environment-specific value out of source code.

Expected categories:

```text id="8asgl8"
APP_ENV
APP_VERSION
DEBUG
DATABASE_URL
CORS_ORIGINS
APP_TIMEZONE

MAX_HEARTS
HEART_REGEN_MINUTES
PRACTICE_RECOVERY_COOLDOWN_MINUTES

SLOW_REQUEST_THRESHOLD_MS
```

Also:

```text id="izp3gf"
NEXT_PUBLIC_API_URL
NEXT_PUBLIC_APP_ENV
```

where appropriate.

---

# 2. Secret management

Search the repository for:

```text
password
secret
token
api_key
private_key
```

There must be **no real credentials in Git**.

Check:

```text id="6f3k0u"
.env
.env.local
.env.production
```

and ensure they are ignored where appropriate.

Create:

```text id="5y5h5m"
.env.example
```

containing placeholders:

```env
APP_ENV=development
APP_VERSION=1.0.0
DATABASE_URL=sqlite:///./app.db
CORS_ORIGINS=http://localhost:3000
```

Never put actual secrets in `.env.example`.

---

# 3. Git secret scan

Perform a repository-wide search for accidental secrets.

Check:

```text id="4rih8g"
Git history
source files
config files
scripts
README
logs
test fixtures
```

If a fake credential exists only as test data, make that explicit.

If a real credential was ever committed, it must be rotated rather than merely deleted.

---

# 4. Production DEBUG behavior

Ensure:

```text id="uo0c5g"
DEBUG=true
```

is only possible in development.

Production:

```text id="ovp6m6"
DEBUG=false
```

Never expose:

```text id="gk7ji7"
stack traces
SQL queries
environment variables
internal paths
```

to clients.

---

# 5. CORS hardening

Replace development-style:

```python
allow_origins=["*"]
```

with environment-driven configuration.

Development:

```text id="yrd2yq"
http://localhost:3000
```

Production:

```text id="v2tc9u"
https://your-frontend-domain
```

Only allow required origins.

Also review:

```text allow_credentials
allow_methods
allow_headers
```

Do not enable everything blindly.

---

# 6. Security headers

Implement appropriate headers.

At minimum consider:

```text id="w5t9f2"
X-Content-Type-Options: nosniff
X-Frame-Options: DENY
Referrer-Policy: strict-origin-when-cross-origin
```

Also consider a suitable:

```text Content-Security-Policy
```

but implement it carefully so it does not break Next.js or browser speech APIs.

Test the actual application after enabling it.

---

# 7. Request size limits

Protect JSON endpoints from unnecessarily large payloads.

Especially:

```text id="yqz0q2"
/answer
/review
/search
/social
```

You don't need huge payloads for this application.

Define sensible limits.

For example:

```text id="kzj0al"
answer payload
→ small bounded size

search query
→ max ~100–200 characters
```

Exact numbers should be based on your actual schemas.

---

# 8. Query parameter validation

Audit:

```text id="dnv9bk"
course_id
lesson_id
skill_id
user_id
limit
offset
period
scope
query
```

Validate:

```text integer IDs > 0
limit bounded
offset >= 0
enum values restricted
strings length limited
```

No endpoint should accept arbitrary unbounded pagination.

---

# 9. Pagination protection

Audit every collection endpoint:

```text id="b9lm8e"
leaderboard
notifications
social feed
search
vocabulary
achievements
quests
```

Use:

```text id="mvrqgk"
default limit
maximum limit
offset/cursor constraints
```

Example:

```text limit=20
max=100
```

A malicious request like:

```text id="u04o2h"
limit=1000000000
```

must be rejected or capped.

---

# 10. Rate-limiting foundation

Do not add Redis yet.

But establish the architecture.

Create:

```text id="n8e6n5"
backend/app/shared/rate_limit.py
```

with a small abstraction:

```text id="gdu3kq"
check()
consume()
reset()
```

The implementation can initially be in-memory for development.

Document that production will move the state to:

```text id="t0j58b"
Redis
```

when multiple replicas exist.

---

# 11. Protect sensitive endpoints

Prioritize:

```text id="v6v9o9"
POST /answer
POST /complete
POST /practice
POST /refill
POST /follow
POST /notifications/read
```

The most sensitive are:

```text id="dq8r7m"
/answer
/complete
/refill
```

because they affect game state.

Don't make rate limiting so aggressive that normal learning breaks.

---

# 12. Idempotency foundation

You already protected duplicate:

```text id="1m8x9a"
answer
lesson completion
achievement
unit reward
course reward
quest reward
```

Now formalize the concept.

For mutation endpoints where retries are likely, consider:

```text id="c0kcyc"
Idempotency-Key
```

especially:

```text id="h5p7f4"
/complete
/refill
```

For the current MVP, application/database constraints remain the primary defense.

---

# 13. Mutation authorization audit

Every state-changing endpoint must derive ownership from:

```text id="1fq8fr"
current user
```

not request bodies.

Audit:

```text id="fjgk9t"
follow
unfollow
answer
complete
practice
refill
read notification
quests
```

Look for request fields such as:

```json id="9x8kjr"
{
  "user_id": 10
}
```

that can bypass ownership.

Remove or ignore those fields where unnecessary.

---

# 14. Business authorization matrix

Document:

| Operation           | Required ownership/business rule    |
| ------------------- | ----------------------------------- |
| Answer exercise     | Current user owns active attempt    |
| Complete lesson     | Current user owns attempt           |
| Start lesson        | Skill must be available/in-progress |
| Practice            | Current user                        |
| Refill              | Current user                        |
| Read notification   | Current user                        |
| Follow              | Current user is follower            |
| View public profile | Public data only                    |
| Read admin overview | Internal/admin only                 |

This becomes part of:

```text id="ov5i9k"
docs/security.md
```

---

# 15. Admin endpoint protection

This is especially important.

We created:

```text id="76j2or"
/api/v1/admin/overview
/ops
```

Do **not** leave this publicly usable in production.

Until authentication exists, make it:

```text id="y4tyfr"
development-only
```

or protect it with a clearly documented internal mechanism.

Before AWS deployment, the production version must require authentication/authorization.

---

# 16. Public/private API classification

Create documentation:

```text id="rk3fm5"
docs/api-access.md
```

Classify:

```text public
learner
admin/internal
development-only
```

This prevents accidentally deploying development APIs publicly.

---

# 17. Database safety

Review database configuration.

Development:

```text id="1yro4y"
SQLite
```

Production architecture later:

```text id="qpmeyx"
PostgreSQL
```

Do not attempt to make SQLite solve distributed production writes.

However, for this assignment/demo:

```text id="f09m22"
keep SQLite
```

until deployment architecture requires otherwise.

Document the migration path.

---

# 18. Database foreign-key enforcement

Ensure SQLite foreign keys are enabled:

```text id="z5j4p7"
PRAGMA foreign_keys = ON
```

Verify tests actually fail when invalid relationships are attempted.

This is easy to forget and important for integrity.

---

# 19. Database migration strategy

If you haven't already, establish a migration strategy.

Preferred:

```text id="q6pmwv"
Alembic
```

or a clearly documented migration mechanism.

The application should not depend on:

```text id="w5j8u3"
drop database
reseed
```

for production schema changes.

You can continue using seed scripts for content, but schema changes should be versioned.

---

# 20. Seed vs migration separation

Clearly separate:

```text id="e8szhw"
migrations
→ schema changes

seed
→ initial/demo content

catalog generator
→ course content generation
```

Do not mix them.

This becomes very important in Docker/Kubernetes.

---

# 21. Dependency audit

Backend:

```bash id="c6z9f5"
pip list
pip-audit
```

Frontend:

```bash id="gb8jx9"
npm audit
```

Review actual vulnerabilities.

Do not blindly run:

```bash
npm audit fix --force
```

because it may break the application.

Update only where safe and test afterwards.

---

# 22. Lock dependencies

Ensure:

```text id="m0b0b0"
package-lock.json
```

and:

```text id="v8pvw5"
Python dependency lock/constraints
```

are committed.

For production builds, dependencies should be reproducible.

---

# 23. Python version consistency

Define the supported version.

For example:

```text id="8mtgkz"
Python 3.12
```

Whatever your actual environment uses.

Document it in:

```text id="0d9qv7"
README
pyproject.toml
Dockerfile later
```

Do not allow local development to silently use incompatible Python versions.

---

# 24. Node/Next version consistency

Record:

```text id="4z7x1k"
Node version
npm version
Next.js version
```

Add:

```text id="1r7jv5"
.nvmrc
```

if appropriate.

This will make Jenkins and Docker builds reproducible.

---

# 25. Production environment validation

Create startup validation.

The backend should detect invalid production config such as:

```text id="o8f6l1"
DEBUG=true
missing DATABASE_URL
invalid CORS
missing required configuration
```

and fail clearly.

Don't start a supposedly production server with broken configuration.

---

# 26. Graceful shutdown

FastAPI should handle shutdown cleanly.

Review:

```text id="6kt4rd"
startup
shutdown
database connections
background resources
```

This becomes important in Kubernetes where pods are terminated regularly.

---

# 27. Request timeout considerations

Identify operations that should not hang indefinitely:

```text id="7e8rv0"
database query
external API
speech/audio future service
```

For this MVP there may not be external requests, but document the pattern.

---

# 28. Security-focused tests

Add tests for:

```text id="2r3iz8"
invalid IDs
invalid pagination
oversized inputs
unauthorized mutation
locked lesson access
cross-user attempt access
admin endpoint protection
CORS
security headers
debug mode behavior
```

---

# 29. Production configuration test

Create a test configuration representing:

```text id="nwt4mt"
APP_ENV=production
DEBUG=false
```

Then run the backend and verify:

```text id="q2x4qn"
health works
API works
CORS correct
stack traces hidden
security headers present
```

---

# 30. Frontend security review

Check:

```text id="kj4s7j"
dangerouslySetInnerHTML
localStorage sensitive data
exposed environment variables
unsafe URL navigation
external links
```

Never store server-side secrets in:

```text id="aqz4y1"
NEXT_PUBLIC_*
```

---

# 31. Client-side storage audit

The browser should only store non-sensitive convenience state, such as:

```text id="mj6jxl"
selected course
UI preferences
```

Do not store:

```text id="tqk8aq"
database credentials
private backend secrets
admin tokens
```

Authentication isn't implemented yet, so don't invent a token-storage system.

---

# 32. Security documentation

Create:

```text id="6cb2td"
docs/security.md
```

Include:

```text id="2ljd7k"
threat model
ownership rules
API access levels
secrets policy
CORS
headers
rate limiting
input validation
database integrity
future auth model
```

---

# 33. Future authentication architecture

Do not implement full authentication this hour.

But document:

```text id="3c51yj"
Frontend
 ↓
Access Token
 ↓
FastAPI auth dependency
 ↓
CurrentUser
 ↓
Domain services
```

Your current demo-user dependency should be designed so replacing it later doesn't require rewriting every router/service.

---

# 34. Security production checklist

Create:

```text id="6q6g0e"
docs/security-checklist.md
```

with:

```text id="o8q1ob"
[ ] no secrets in Git
[ ] production DEBUG=false
[ ] CORS restricted
[ ] security headers
[ ] request limits
[ ] pagination limits
[ ] ownership validation
[ ] admin protection
[ ] dependency scan
[ ] database foreign keys
[ ] migrations
[ ] reproducible dependencies
[ ] logs safe
```

---

# 35. Phase 33 acceptance criteria

```text id="0hyh1s"
[ ] Production config is environment-driven
[ ] .env.example exists
[ ] No real secrets are committed
[ ] Production DEBUG is disabled
[ ] CORS is restricted
[ ] Security headers exist
[ ] Request payload limits exist
[ ] Query parameters are validated
[ ] Pagination has safe limits
[ ] Rate-limit abstraction exists
[ ] Critical mutations identified/protected
[ ] Ownership checks audited
[ ] Admin endpoints are protected/development-only
[ ] SQLite foreign keys are enforced
[ ] Migration strategy exists
[ ] Seed/migration responsibilities are separated
[ ] Dependency audit completed
[ ] Dependency versions are reproducible
[ ] Python/Node versions are documented
[ ] Startup configuration validation exists
[ ] Graceful shutdown is handled
[ ] Security tests exist
[ ] Production configuration test passes
[ ] docs/security.md exists
[ ] docs/security-checklist.md exists
```

---

# What this phase does NOT implement

```text id="m9km6k"
❌ Full user authentication
❌ OAuth
❌ JWT implementation
❌ Redis
❌ Production rate-limit storage
❌ PostgreSQL migration
❌ WAF
❌ Cloud secrets manager
❌ Docker
❌ Jenkins
❌ Kubernetes
❌ AWS
```

---

# After Hour 15

We've reached an important boundary.

The product side is now very deep:

```text id="m1g4fp"
Courses
Units
Skills
Lessons
Exercises
Mastery
Review
Quests
Achievements
Social
Notifications
Search
Vocabulary
```

And the operational side now has:

```text id="z5j0t9"
Health
Metrics
Logs
Audit
Security
Configuration
```

So the remaining hours should stop expanding the application indefinitely.

The next sequence should be:

```text
Hour 16 → Final architecture review + production readiness
Hour 17 → Final automated regression / release candidate
Hour 18 → Docker
Hour 19 → Jenkins CI/CD
Hour 20 → Kubernetes
Hour 21 → AWS production deployment
```

That gives the last six hours a very clear **DevOps-to-production trajectory**, which is much more valuable for this project than continuing to add increasingly minor UI features.
